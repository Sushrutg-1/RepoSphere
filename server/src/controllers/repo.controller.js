export const createRepo = (req, res) => {
  res.status(200).json({ message: "register user is running...." });
};

export const getAllRepos = (req, res) => {
  res.status(200).json({ message: "register user is running...." });
};

export const getRepoByName = (req, res) => {
  res.status(200).json({ message: "register user is running...." });
};
export const getRepoByUserId = (req, res) => {
  res.status(200).json({ message: "register user is running...." });
};

export const getRepoById = (req, res) => {
  res.status(200).json({ message: "register user is running...." });
};

export const updateRepo = (req, res) => {
  res.status(200).json({ message: "register user is running...." });
};

export const deleteRepo = (req, res) => {
  res.status(200).json({ message: "register user is running...." });
};

export const toggeleRepoVisibility = (req, res) => {
  res.status(200).json({ message: "register user is running...." });
};
